# NMAP 筆記

## NMAP功能
- 主機發現
- 端口掃描(服務版本偵測)

## 主機發現: 內網主機掃描
```
nmap -sn [IP]
```

### 目標IP表示方式
- 範圍IP表示: 192.168.0.1-10
- IP子網掃描: 192.168.0.1/24 等價於 192.168.0.0-255
- hostname: example.com

### 掃描本地網路 - 內網
- 假設在同個網域(乙太、wifi連線)之下IP位置為 192.168.66.89，我們可以利用nmap掃描目前在同一個網段的主機
```
nmap -sn 192.168.66.0/24
```
- Ping 掃描 - 僅主機發現

### 列表掃描
- 列出目標而無需掃描
```
nmap -sL 192.168.0.1/24
```

## 端口掃描

### 掃描TCP端口
- 連線掃描 -sT: 它會嘗試與每個目標 TCP 連接埠完成 TCP 三次握手。
- 同步掃描 -sS: SYN 掃描只執行第一步握手，好處是連接從未完整建立，預計會產生更少的日誌，因此它被認為是相對隱藏的掃描。

### 掃描UDP端口
- -sU: 提供掃描選項UDP服務。

### 端口限制
- Nmap 預設掃描最常用的 1000 個連接埠。
    - 快速模式 -F: 它會掃描 100 個最常用的連接埠
    - -p[range]: 允許您指定要掃描的連接埠範圍。 -p10-1024
        - -p-25: 等效 -p1-25
        - -p-: 等效 -p1-65535

- 提示：最常用的服務使用 1 到 1024 之間的連接埠號，用於 UDP 或 TCP 協定。這些連接埠也稱為知名連接埠。可以使用-p1-1023掃描工具尋找知名連接埠。
- 強制掃描 -Pn: 將所有主機視為在線，並掃描每個主機，包括那些在主機發現階段沒有回應的主機 

### 版本偵測
- 作業系統版本偵測: -O
- 服務版本偵測: -sV
- 整合掃描: -A = -O + -sV

## 效率控制
- T<0-5>: 時間模板－多疑(0)、鬼鬼祟祟(1)、禮貌(2)、正常(3)、好鬥(4)、瘋狂(5)
- --min-parallelism/--max-parallelism <numprobes>: 設定主機組同時活動的 TCP 和 UDP 連接埠偵測的最小值和最大值
- --min-rate/--max-rate <number>: 控制nmap發送資料包的最小和最大速率。(單位 NPS)
- --host-timeout <time>: 等待目標主機的最長時間

## 輸出/儲存
- 主要關注兩個面向:
    - 掃描過程中顯示附加訊息
    - 選擇儲存掃描報告的文件格式

### 詳細程度和調試
- -v<1-4>: 提供詳細的輸出
- -d<1-9>: 偵錯等級輸出報告

### 儲存掃描報告
- 掃描報告輸出格式:
    - -oN <filename>: 正常輸出
    - -oX <filename>: XML輸出
    - -oG <filename>: 可輸出(對andgrep有用) grepawk
    - -oA <basename>: 輸出所有主要格式